{
    "name": "@justdoit/database",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "db:generate": "npx prisma generate",
        "db:push": "npx prisma db push --skip-generate",
        "build": "tsc && npx prisma generate"
    },
    "dependencies": {
        "@prisma/client": "latest",
        "prisma": "^5.8.0"
    }
}
